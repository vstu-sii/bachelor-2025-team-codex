@'
# Quality Report — Sprint 5 (SA/PO)

## 1) Use-case validation matrix (на основе Lab 5)
Источник: UC-1..UC-5 Use Cases.

| Use Case | Статус | Evidence (что показать) | Acceptance Criteria (ключевое) |
|---|---|---|---|
| UC-1 Create Deck | TBD | UI: импорт файла + preview + сохранение | <60s; >=30 cards; понятные ошибки |
| UC-2 Edit Deck | TBD | UI: редактирование/удаление/merge дублей | изменения сохраняются; без потери данных |
| UC-3 SRS Review | TBD | UI: сессия + grading + сохранение прогресса | прогресс не теряется; интервалы обновляются |
| UC-4 Stats | TBD | UI: статистика + период + фильтры | корректные данные; без задержек |
| UC-5 Profile | TBD | UI: изменение настроек + валидация | валидация; подтверждение критичных действий |

## 2) Quality Gates (минимальный набор)
- GitHub PR: сборка/тесты проходят в CI
- Docker: сервисы запускаются через compose, логи доступны через docker logs
- Безопасность: базовая проверка по OWASP Top 10 (инъекции, auth, управление сессиями, утечки секретов)

## 3) Performance (легкий benchmark для Demo)
- UC-1: время обработки файла (цель < 60 сек)
- API: базовые ответы backend без заметных таймаутов
- UI: основные страницы открываются стабильно

## 4) Logging & Monitoring (что считаем достаточным)
- Логи: приложение пишет в stdout/stderr, контейнер собирает логи
- Ошибки: понятные сообщения пользователю + технический лог для диагностики

## 5) Известные риски/техдолг (пример)
- Внешний AI может быть недоступен (таймауты/ретраи/сообщения пользователю)
- Секреты должны храниться только в Actions Secrets
- План восстановления БД задокументирован (backup/PITR)
'@ | Set-Content -Encoding UTF8 docs/quality-report.md
